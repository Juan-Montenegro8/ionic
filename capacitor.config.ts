import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dajo4.dajo4',
  appName: 'dajo4',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
